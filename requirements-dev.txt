# AutoLedger — development & test dependencies
# Installed on top of requirements.txt for local/CI test runs.
# Production images (Dockerfile) deliberately do NOT include these.

-r requirements.txt

pytest==8.2.2      # Test runner
ruff==0.5.1        # Linter + formatter (also referenced by the Makefile)
